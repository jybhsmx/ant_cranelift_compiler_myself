extern "C" func malloc(size: usize) -> *u8;
extern "C" func printf(s: str, ...) -> i32;

func main() -> i32 {
    printf("memory: i am free.\n");
    
    let ptr = malloc(32usize);

    printf("dirty mem ptr %p\n", ptr);
    printf("dirty mem val %d\n", *ptr);

    0i32
}